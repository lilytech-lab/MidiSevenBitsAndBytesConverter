@inherits LayoutComponentBase

<MudThemeProvider />
<MudPopoverProvider />
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
    <MudAppBar Elevation="1" Dense>
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@ToggleDrawer" />
        <MudText Typo="Typo.h5" Class="pl-16">MIDI Seven Bits and Bytes Converter</MudText>
    </MudAppBar>
    <MudDrawer @bind-Open="@isOpeningMenu" Variant="@DrawerVariant.Responsive" Color="Color.Primary" Elevation="1">
        <MudDrawerHeader>
            <MudText Typo="Typo.h6">MIDI Seven Bits and Bytes Converter</MudText>
        </MudDrawerHeader>
        <NavMenu />
    </MudDrawer>

    <MudMainContent Class="pt-16 px-2">
        <MudContainer Class="mt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool isOpeningMenu = false;

    private void ToggleDrawer()
    {
        this.isOpeningMenu = !this.isOpeningMenu;
    }
}
